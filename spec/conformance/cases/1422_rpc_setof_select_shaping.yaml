id: 1422
feature: rpc/shape/select
request:
  method: GET
  path: /rpc/getproject?id=1&select=id,name
  headers: { Accept: application/json }
schema: rpc
preconditions: []
expect:
  status: 200
  headers:
    Content-Type: application/json; charset=utf-8
  body_exact:
    - { "id": 1, "name": "Windows 7" }
notes: >
  Directly traces the "select works on the first level" it-block
  (GET /rpc/getproject?id=1&select=id,name) at RpcSpec.hs#L314; the GET request
  line is L317. select shapes the columns of a table-valued proc result the same
  way as on a table.
source: https://raw.githubusercontent.com/PostgREST/postgrest/v14.12/test/spec/Feature/Query/RpcSpec.hs#L317
