id: 10215
feature: operators/not/logic_tree
request:
  method: GET
  path: /entities?and=(arr.not.cd.{1},or(id.not.lt.1,id.not.gte.3))&select=id
  headers: { Accept: application/json }
schema: operators
preconditions: []
expect:
  status: 200
  headers:
    Content-Type: application/json; charset=utf-8
  body_exact:
    - { "id": 2 }
    - { "id": 3 }
notes: >
  `not` negates lt, gte and cd inside a nested and()/or() logic tree.
  Mirrors AndOrParamsSpec.hs 'operators with not' / 'lt, gte, cd can be
  negated'.
source: https://raw.githubusercontent.com/PostgREST/postgrest/v16.0/test/spec/Feature/Query/AndOrParamsSpec.hs#L190
