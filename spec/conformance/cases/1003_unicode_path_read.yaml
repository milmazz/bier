id: 1003
feature: url_grammar/percent-encoding/unicode-path
request:
  method: GET
  path: /%D9%85%D9%88%D8%A7%D8%B1%D8%AF
  headers: { Accept: application/json }
schema: unicode
preconditions: []
expect:
  status: 200
  headers:
    Content-Type: application/json; charset=utf-8
  body_exact: []
notes: "Percent-encoded path decodes to the unicode relation موارد in exposed schema تست. The schema name تست is verified inline in v16.0 at UnicodeSpec.hs#L15 (baseCfg { configDbSchemas = fromList [\"تست\"] }) — v16 dropped the named testUnicodeCfg helper that v14.12 kept in SpecHelper.hs — and the schema/table are created at fixtures/schema.sql#L10/L187. Mirrors UnicodeSpec read (get at L18, shouldRespondWith \"[]\" at L19)."
source: https://raw.githubusercontent.com/PostgREST/postgrest/v16.0/test/spec/Feature/Query/UnicodeSpec.hs#L18
