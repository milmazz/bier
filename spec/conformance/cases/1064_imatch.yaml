id: 1064
feature: operators/imatch
request:
  method: GET
  path: /simple_pk?k=imatch.^xy&order=extra.asc
  headers: { Accept: application/json }
schema: operators
preconditions: []
expect:
  status: 200
  headers:
    Content-Type: application/json; charset=utf-8
  body_exact:
    - { "k": "xyyx", "extra": "u" }
    - { "k": "xYYx", "extra": "v" }
notes: "imatch -> POSIX regex `~*` (case-insensitive). Mirrors QuerySpec.hs 'matches with ~*'."
source: https://raw.githubusercontent.com/PostgREST/postgrest/v14.12/test/spec/Feature/Query/QuerySpec.hs#L166
