id: 1718
feature: config/validation/jwt-secret-invalid-base64
request:
  kind: cli
  flag: "--dump-config"
schema: config
config:
  env:
    PGRST_JWT_SECRET_IS_BASE64: "true"
    PGRST_JWT_SECRET: "no base-64!"
preconditions: []
expect:
  exit_code: nonzero
notes: "jwt-secret-is-base64=true with a non-base64 secret aborts startup. Mirrors cli fixture 'invalid jwt-secret not base64' (expect: error)."
source: https://raw.githubusercontent.com/PostgREST/postgrest/v14.12/test/io/fixtures/fixtures.yaml#L64
