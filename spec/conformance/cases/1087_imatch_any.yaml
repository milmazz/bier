id: 1087
feature: operators/quantifier/imatch_any
request:
  method: GET
  path: /articles?body=imatch(any).{stop,thing}&select=id
  headers: { Accept: application/json }
schema: operators
preconditions: []
expect:
  status: 200
  headers:
    Content-Type: application/json; charset=utf-8
  body_exact:
    - { "id": 1 }
    - { "id": 2 }
notes: "imatch(any) -> `body ~* ANY('{stop,thing}')`. Mirrors QuerySpec.hs any/all quantifiers 'works with the match/imatch operator'."
source: https://raw.githubusercontent.com/PostgREST/postgrest/v14.12/test/spec/Feature/Query/QuerySpec.hs#L1527
