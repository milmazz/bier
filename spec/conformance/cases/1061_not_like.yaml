id: 1061
feature: operators/not/like
request:
  method: GET
  path: /simple_pk?k=not.like.*yx
  headers: { Accept: application/json }
schema: operators
preconditions: []
expect:
  status: 200
  body_exact: [{ "k": "xYYx", "extra": "v" }]
notes: "not.like negates the pattern match. Mirrors QuerySpec.hs 'matches with like using not operator'."
source: https://raw.githubusercontent.com/PostgREST/postgrest/v14.12/test/spec/Feature/Query/QuerySpec.hs#L139
