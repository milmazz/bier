id: 1726
feature: config/dump-config/stable
request:
  kind: cli
  flag: "--dump-config"
schema: config
config:
  env:
    PGRST_DB_MAX_ROWS: "1000"
    PGRST_SERVER_PORT: "80"
    PGRST_LOG_LEVEL: "info"
preconditions: []
expect:
  exit_code: 0
  dump_reparse_stable: true
notes: "A dumped config, written back to a file and re-dumped, must be byte-identical. Mirrors test_cli.py:test_stable_config."
source: https://raw.githubusercontent.com/PostgREST/postgrest/v14.12/test/io/test_cli.py#L189
