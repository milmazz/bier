id: 1269
feature: pagination/count/exact_empty_body
request:
  method: GET
  path: /items?id=eq.0
  headers:
    Accept: application/json
    Prefer: count=exact
schema: pagination
preconditions: []
expect:
  status: 200
  headers:
    Content-Range: "*/0"
  body_exact: []
notes: >
  count=exact over a filtered-out result returns [] with Content-Range '*/0',
  status 200 (window equals total of 0). Mirrors the it-block at
  RangeSpec.hs:153 — inside the context "count with an empty body" at :152 —
  whose request is at :154, whose 'Prefer: count=exact' header is at :155 and
  whose asserted `"Content-Range" <:> "*/0"` is at :158.
source: https://raw.githubusercontent.com/PostgREST/postgrest/v16.0/test/spec/Feature/Query/RangeSpec.hs#L153
