id: 1804
feature: domain_representations/read/format_and_rename
request:
  method: GET
  path: /datarep_todos?select=id,clr:label_color&id=lt.4
  headers: { Accept: application/json }
schema: domain_representations
preconditions: []
expect:
  status: 200
  headers:
    Content-Type: application/json; charset=utf-8
  body_exact:
    [
      { "id": 1, "clr": "#000000" },
      { "id": 2, "clr": "#000100" },
      { "id": 3, "clr": "#01E240" },
    ]
notes: "Renaming a domain column still applies its AS json cast; only the JSON key changes. Mirrors QuerySpec.hs 'formats and allows rename'."
source: https://raw.githubusercontent.com/PostgREST/postgrest/v14.12/test/spec/Feature/Query/QuerySpec.hs#L1562
