id: 1017
feature: url_grammar/percent-encoding/query-value-space
request:
  method: GET
  path: /parents?name=eq.parent%20v1-1
  headers:
    Accept: application/json
    Accept-Profile: v1
schema: multi
preconditions: []
expect:
  status: 200
  headers:
    Content-Type: application/json; charset=utf-8
  body_exact: [{ "id": 1, "name": "parent v1-1" }]
notes: "%20 in a filter value decodes to a space (parseQueryReplacePlus True, v16.0 QueryParams.hs#L157). Matches the row whose name contains a space. %20-space behavior mirrored by the QuerySpec tsearch cases at L203-L213."
source: https://raw.githubusercontent.com/PostgREST/postgrest/v16.0/src/library/PostgREST/ApiRequest/QueryParams.hs#L157
