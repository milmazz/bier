# HTTP benchmark: Bier vs PostgREST v14.12

> Generated by `bench/http/run.sh` on 2026-07-11. Do not edit by hand.

## Environment

| | |
|---|---|
| Hardware | Apple M1 Max, 64 GB |
| OS | macOS 26.5.1 |
| Elixir / OTP | 1.19.5 / 28 |
| PostgreSQL | 17.10 (Homebrew) |
| PostgREST | v14.12 |
| Bier | 0.1.1-dev |
| k6 | k6 v2.1.0 (commit/devel, go1.26.4, darwin/arm64) |

## R1 — single row by PK

Request: `GET /items?id=eq.<id>` — latency measured at a shared arrival rate of **1678 req/s**.

| Metric | Bier | PostgREST | Bier / PostgREST |
|---|---|---|---|
| Max throughput (req/s) | 17433.38 | 4195.24 | 4.16x |
| p50 latency (ms) | 0.52 (0.52–0.53) | 0.52 (0.52–0.52) | 1.01x (lower is better) |
| p90 latency (ms) | 0.68 (0.68–0.69) | 0.84 (0.62–0.88) | 0.81x (lower is better) |
| p99 latency (ms) | 10.09 (7.54–11.56) | 292.11 (64.53–467.30) | 0.03x (lower is better) |

## R2 — filtered 25-row page

Request: `GET /items?category=eq.<cat>&order=id.desc&limit=25` — latency measured at a shared arrival rate of **1136 req/s**.

| Metric | Bier | PostgREST | Bier / PostgREST |
|---|---|---|---|
| Max throughput (req/s) | 2842.01 | 3561.32 | 0.80x |
| p50 latency (ms) | 2.44 (2.42–2.44) | 0.85 (0.85–0.88) | 2.86x (lower is better) |
| p90 latency (ms) | 2.58 (2.56–2.58) | 1.36 (1.36–3.47) | 1.90x (lower is better) |
| p99 latency (ms) | 3.70 (3.64–3.71) | 6.31 (6.20–929.68) | 0.59x (lower is better) |

## M1 — insert

Request: `POST /events` (`Prefer: return=minimal`) — latency measured at a shared arrival rate of **1120 req/s**.

| Metric | Bier | PostgREST | Bier / PostgREST |
|---|---|---|---|
| Max throughput (req/s) | 8504.27 | 3736.02 | 2.28x |
| p50 latency (ms) | 0.61 (0.61–0.61) | 0.80 (0.79–0.80) | 0.76x (lower is better) |
| p90 latency (ms) | 1.19 (1.18–1.21) | 0.99 (0.99–0.99) | 1.20x (lower is better) |
| p99 latency (ms) | 2.65 (2.46–4.56) | 4.62 (4.37–4.65) | 0.57x (lower is better) |

## M2 — update by PK

Request: `PATCH /events?id=eq.<id>` — latency measured at a shared arrival rate of **1112 req/s**.

| Metric | Bier | PostgREST | Bier / PostgREST |
|---|---|---|---|
| Max throughput (req/s) | 8641.56 | 3707.95 | 2.33x |
| p50 latency (ms) | 0.61 (0.60–0.61) | 0.78 (0.78–0.78) | 0.78x (lower is better) |
| p90 latency (ms) | 1.16 (1.16–1.18) | 1.02 (1.02–1.11) | 1.14x (lower is better) |
| p99 latency (ms) | 2.32 (2.29–3.17) | 6.32 (5.12–30.04) | 0.37x (lower is better) |

## Methodology

Both servers ran natively on the machine above against the same local
PostgreSQL and the `bier_bench` database (`bench/http/fixtures.sql`:
100k-row `bench.items`, `bench.events` reset to a 10k-row baseline before
every round). Config parity: pool size 10, schema `bench`, anon role
`bench_anon`, no JWT, logging off, no compression, HTTP/1.1 keep-alive.

Per scenario: a closed-loop ceiling stage (constant VUs, once per server)
found max throughput; then 3 interleaved A/B rounds of an
open-loop `constant-arrival-rate` stage at
40% (reads) / 30% (writes) of the slower server's ceiling
(the shared rate above) measured latency, immune to coordinated
omission. Each stage: 10s discarded warmup +
60s measurement; the latency stage runs warmup and
measurement as one continuous arrival stream (no idle gap between k6
runs — a gap was observed to stall PostgREST's GHC idle GC) and reports
only the measured phase. Only the measured server ran during a
stage. Latency cells show median across rounds (min–max). Any non-2xx
response, dropped iteration, or >5% miss of the shared arrival rate
aborts the run. Full details:
`docs/superpowers/specs/2026-07-09-http-benchmark-design.md`.
