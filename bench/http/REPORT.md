# HTTP benchmark: Bier vs PostgREST v14.12

> Generated by `bench/http/run.sh` on 2026-07-12. Do not edit by hand.

## Environment

| | |
|---|---|
| Hardware | Apple M1 Max, 64 GB |
| OS | macOS 26.5.1 |
| Elixir / OTP | 1.20.2 / 29 |
| PostgreSQL | 17.10 (Homebrew) |
| PostgREST | v14.12 |
| Bier | 0.1.1-dev |
| k6 | k6 v2.1.0 (commit/devel, go1.26.4, darwin/arm64) |

## R1 — single row by PK

Request: `GET /items?id=eq.<id>` — latency measured at a shared arrival rate of **1330 req/s**.

| Metric | Bier | PostgREST | Bier / PostgREST |
|---|---|---|---|
| Max throughput (req/s) | 18543.92 | 4434.36 | 4.18x |
| p50 latency (ms) | 0.58 (0.56–0.65) | 0.68 (0.68–0.68) | 0.86x (lower is better) |
| p90 latency (ms) | 0.78 (0.76–0.81) | 0.84 (0.83–1.21) | 0.93x (lower is better) |
| p99 latency (ms) | 3.87 (3.79–5.10) | 4.26 (4.18–829.81) | 0.91x (lower is better) |

## R2 — filtered 25-row page

Request: `GET /items?category=eq.<cat>&order=id.desc&limit=25` — latency measured at a shared arrival rate of **1067 req/s**.

| Metric | Bier | PostgREST | Bier / PostgREST |
|---|---|---|---|
| Max throughput (req/s) | 13698.48 | 3556.87 | 3.85x |
| p50 latency (ms) | 1.14 (1.12–1.14) | 0.95 (0.95–0.96) | 1.19x (lower is better) |
| p90 latency (ms) | 2.51 (2.35–2.69) | 1.55 (1.53–1.60) | 1.62x (lower is better) |
| p99 latency (ms) | 14.67 (9.39–15.67) | 6.58 (6.39–8.77) | 2.23x (lower is better) |

## M1 — insert

Request: `POST /events` (`Prefer: return=minimal`) — latency measured at a shared arrival rate of **1133 req/s**.

| Metric | Bier | PostgREST | Bier / PostgREST |
|---|---|---|---|
| Max throughput (req/s) | 8561.27 | 3777.28 | 2.27x |
| p50 latency (ms) | 0.59 (0.59–0.60) | 0.77 (0.77–0.78) | 0.77x (lower is better) |
| p90 latency (ms) | 1.16 (1.15–1.19) | 1.00 (0.99–1.04) | 1.16x (lower is better) |
| p99 latency (ms) | 2.33 (2.09–3.89) | 5.84 (4.68–68.31) | 0.40x (lower is better) |

## M2 — update by PK

Request: `PATCH /events?id=eq.<id>` — latency measured at a shared arrival rate of **1111 req/s**.

| Metric | Bier | PostgREST | Bier / PostgREST |
|---|---|---|---|
| Max throughput (req/s) | 8533.25 | 3704.28 | 2.30x |
| p50 latency (ms) | 0.61 (0.60–0.64) | 0.78 (0.78–0.79) | 0.78x (lower is better) |
| p90 latency (ms) | 1.20 (1.17–1.44) | 1.01 (1.00–1.02) | 1.19x (lower is better) |
| p99 latency (ms) | 6.45 (3.51–134.54) | 4.59 (4.28–5.56) | 1.41x (lower is better) |

## Methodology

Both servers ran natively on the machine above against the same local
PostgreSQL and the `bier_bench` database (`bench/http/fixtures.sql`:
100k-row `bench.items`, `bench.events` reset to a 10k-row baseline before
every round). Config parity: pool size 10, schema `bench`, anon role
`bench_anon`, no JWT, logging off, no compression, HTTP/1.1 keep-alive.

Per scenario: a closed-loop ceiling stage (constant VUs, once per server)
found max throughput; then 3 interleaved A/B rounds of an
open-loop `constant-arrival-rate` stage at
30% (reads) / 30% (writes) of the slower server's ceiling
(the shared rate above) measured latency, immune to coordinated
omission. Each stage: 10s discarded warmup +
60s measurement; the latency stage runs warmup and
measurement as one continuous arrival stream (no idle gap between k6
runs — a gap was observed to stall PostgREST's GHC idle GC) and reports
only the measured phase. Only the measured server ran during a
stage. Latency cells show median across rounds (min–max). Any non-2xx
response, dropped iteration, or >5% miss of the shared arrival rate
aborts the run. Full details:
`docs/superpowers/specs/2026-07-09-http-benchmark-design.md`.
