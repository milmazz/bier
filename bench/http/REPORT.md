# HTTP benchmark: Bier vs PostgREST v14.12

> Generated by `bench/http/run.sh` on 2026-07-12. Do not edit by hand.

## Environment

| | |
|---|---|
| Hardware | Apple M1 Max, 64 GB |
| OS | macOS 26.5.1 |
| Elixir / OTP | 1.20.2 / 29 |
| PostgreSQL | 17.10 (Homebrew) |
| PostgREST | v14.12 |
| Bier | 0.1.1-dev |
| k6 | k6 v2.1.0 (commit/devel, go1.26.4, darwin/arm64) |

## R1 — single row by PK

Request: `GET /items?id=eq.<id>` — latency measured at a shared arrival rate of **1365 req/s**.

| Metric | Bier | PostgREST | Bier / PostgREST |
|---|---|---|---|
| Max throughput (req/s) | 18689.62 | 4551.32 | 4.11x |
| p50 latency (ms) | 0.60 (0.53–0.60) | 0.62 (0.62–0.63) | 0.95x (lower is better) |
| p90 latency (ms) | 0.81 (0.77–0.81) | 0.84 (0.83–1.37) | 0.97x (lower is better) |
| p99 latency (ms) | 10.78 (10.06–13.49) | 10.06 (8.94–851.07) | 1.07x (lower is better) |

## R2 — filtered 25-row page

Request: `GET /items?category=eq.<cat>&order=id.desc&limit=25` — latency measured at a shared arrival rate of **1088 req/s**.

| Metric | Bier | PostgREST | Bier / PostgREST |
|---|---|---|---|
| Max throughput (req/s) | 13822.50 | 3628.72 | 3.81x |
| p50 latency (ms) | 1.12 (1.10–1.14) | 0.90 (0.89–0.93) | 1.25x (lower is better) |
| p90 latency (ms) | 2.51 (2.34–2.63) | 1.67 (1.52–2.48) | 1.50x (lower is better) |
| p99 latency (ms) | 10.43 (10.28–12.86) | 14.96 (7.73–898.14) | 0.70x (lower is better) |

## M1 — insert

Request: `POST /events` (`Prefer: return=minimal`) — latency measured at a shared arrival rate of **1145 req/s**.

| Metric | Bier | PostgREST | Bier / PostgREST |
|---|---|---|---|
| Max throughput (req/s) | 8601.69 | 3817.58 | 2.25x |
| p50 latency (ms) | 0.59 (0.59–0.59) | 0.75 (0.75–0.75) | 0.79x (lower is better) |
| p90 latency (ms) | 1.16 (1.13–1.18) | 0.98 (0.98–1.10) | 1.19x (lower is better) |
| p99 latency (ms) | 3.43 (2.04–5.22) | 4.54 (4.37–225.76) | 0.76x (lower is better) |

## M2 — update by PK

Request: `PATCH /events?id=eq.<id>` — latency measured at a shared arrival rate of **1142 req/s**.

| Metric | Bier | PostgREST | Bier / PostgREST |
|---|---|---|---|
| Max throughput (req/s) | 8738.21 | 3807.46 | 2.30x |
| p50 latency (ms) | 0.59 (0.59–0.59) | 0.77 (0.76–0.77) | 0.77x (lower is better) |
| p90 latency (ms) | 1.15 (1.14–1.16) | 1.01 (0.99–1.02) | 1.13x (lower is better) |
| p99 latency (ms) | 2.38 (2.36–3.47) | 6.22 (5.90–12.19) | 0.38x (lower is better) |

## Methodology

Both servers ran natively on the machine above against the same local
PostgreSQL and the `bier_bench` database (`bench/http/fixtures.sql`:
100k-row `bench.items`, `bench.events` reset to a 10k-row baseline before
every round). Config parity: pool size 10, schema `bench`, anon role
`bench_anon`, no JWT, logging off, no compression, HTTP/1.1 keep-alive.

Per scenario: a closed-loop ceiling stage (constant VUs, once per server)
found max throughput; then 3 interleaved A/B rounds of an
open-loop `constant-arrival-rate` stage at
30% (reads) / 30% (writes) of the slower server's ceiling
(the shared rate above) measured latency, immune to coordinated
omission. Each stage: 10s discarded warmup +
60s measurement; the latency stage runs warmup and
measurement as one continuous arrival stream (no idle gap between k6
runs — a gap was observed to stall PostgREST's GHC idle GC) and reports
only the measured phase. Only the measured server ran during a
stage. Latency cells show median across rounds (min–max). Any non-2xx
response, dropped iteration, or >5% miss of the shared arrival rate
aborts the run. Full details:
`docs/superpowers/specs/2026-07-09-http-benchmark-design.md`.
