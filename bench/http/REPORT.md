# HTTP benchmark: Bier vs PostgREST v14.12

> Generated by `bench/http/run.sh` on 2026-07-09. Do not edit by hand.

> **SMOKE RUN** — shortened windows; numbers are pipeline-validation only, not citable.

## Environment

| | |
|---|---|
| Hardware | Apple M1 Max, 64 GB |
| OS | macOS 26.5.1 |
| Elixir / OTP | 1.19.5 / 28 |
| PostgreSQL | 17.10 (Homebrew) |
| PostgREST | v14.12 |
| Bier | 0.1.1-dev |
| k6 | k6 v2.1.0 (commit/devel, go1.26.4, darwin/arm64) |

## R1 — single row by PK

Request: `GET /items?id=eq.<id>` — latency measured at a shared arrival rate of **2027 req/s**.

| Metric | Bier | PostgREST | Bier / PostgREST |
|---|---|---|---|
| Max throughput (req/s) | 15158.59 | 5068.53 | 2.99x |
| p50 latency (ms) | 0.51 (0.51–0.51) | 0.32 (0.32–0.32) | 1.61x (lower is better) |
| p90 latency (ms) | 4.31 (4.31–4.31) | 0.62 (0.62–0.62) | 6.92x (lower is better) |
| p99 latency (ms) | 46.15 (46.15–46.15) | 20.08 (20.08–20.08) | 2.30x (lower is better) |

## R2 — filtered 25-row page

Request: `GET /items?category=eq.<cat>&order=id.desc&limit=25` — latency measured at a shared arrival rate of **1151 req/s**.

| Metric | Bier | PostgREST | Bier / PostgREST |
|---|---|---|---|
| Max throughput (req/s) | 2879.43 | 3957.47 | 0.73x |
| p50 latency (ms) | 2.44 (2.44–2.44) | 0.84 (0.84–0.84) | 2.90x (lower is better) |
| p90 latency (ms) | 2.72 (2.72–2.72) | 1.49 (1.49–1.49) | 1.83x (lower is better) |
| p99 latency (ms) | 7.28 (7.28–7.28) | 54.98 (54.98–54.98) | 0.13x (lower is better) |

## M1 — insert

Request: `POST /events` (`Prefer: return=minimal`) — latency measured at a shared arrival rate of **2084 req/s**.

| Metric | Bier | PostgREST | Bier / PostgREST |
|---|---|---|---|
| Max throughput (req/s) | 8051.01 | 5210.62 | 1.55x |
| p50 latency (ms) | 0.78 (0.78–0.78) | 0.40 (0.40–0.40) | 1.96x (lower is better) |
| p90 latency (ms) | 0.97 (0.97–0.97) | 0.73 (0.73–0.73) | 1.32x (lower is better) |
| p99 latency (ms) | 3.52 (3.52–3.52) | 30.25 (30.25–30.25) | 0.12x (lower is better) |

## M2 — update by PK

Request: `PATCH /events?id=eq.<id>` — latency measured at a shared arrival rate of **1818 req/s**.

| Metric | Bier | PostgREST | Bier / PostgREST |
|---|---|---|---|
| Max throughput (req/s) | 7885.27 | 4546.14 | 1.73x |
| p50 latency (ms) | 0.74 (0.74–0.74) | 0.39 (0.39–0.39) | 1.88x (lower is better) |
| p90 latency (ms) | 0.99 (0.99–0.99) | 0.75 (0.75–0.75) | 1.31x (lower is better) |
| p99 latency (ms) | 8.28 (8.28–8.28) | 6.18 (6.18–6.18) | 1.34x (lower is better) |

## Methodology

Both servers ran natively on the machine above against the same local
PostgreSQL and the `bier_bench` database (`bench/http/fixtures.sql`:
100k-row `bench.items`, `bench.events` reset to a 10k-row baseline before
every round). Config parity: pool size 10, schema `bench`, anon role
`bench_anon`, no JWT, logging off, no compression, HTTP/1.1 keep-alive.

Per scenario: a closed-loop ceiling stage (constant VUs, once per server)
found max throughput; then 1 interleaved A/B rounds of an
open-loop `constant-arrival-rate` stage at
40% of the slower server's ceiling
(the shared rate above) measured latency, immune to coordinated
omission. Each stage: 2s discarded warmup +
5s measurement; the latency stage runs warmup and
measurement as one continuous arrival stream (no idle gap between k6
runs — a gap was observed to stall PostgREST's GHC idle GC) and reports
only the measured phase. Only the measured server ran during a
stage. Latency cells show median across rounds (min–max). Any non-2xx
response, dropped iteration, or >5% miss of the shared arrival rate
aborts the run. Full details:
`docs/superpowers/specs/2026-07-09-http-benchmark-design.md`.
